Context for src/hooks/__tests__/integration/useAdvancedAlarms.integration.test.tsx syntax error:

Lines 143-182:
   143	    mockSubscriptionService.getFeatureAccess.mockResolvedValue({
   144	      features: {
   145	        advanced_alarms: {
   146	          hasAccess: userTier !== 'free',
   147	          usageLimit: userTier === 'basic' ? 10 : null,
   148	          usageCount: mockAlarms.length,
   149	          upgradeRequired: userTier === 'free' ? 'basic' : null
   150	        },
   151	        conditional_rules: {
   152	          hasAccess: userTier === 'pro',
   153	          upgradeRequired: userTier !== 'pro' ? 'pro' : null
   154	        },
   155	        location_triggers: {
   156	          hasAccess: userTier === 'pro',
   157	          upgradeRequired: userTier !== 'pro' ? 'pro' : null
   158	        }
   159	      }
   160	    });
   161	
   162	      // Service is now imported at the top
   163	    mockScheduler.scheduleAlarm.mockResolvedValue({ success: true });    <-- ERROR: mockScheduler not defined
   164	    mockScheduler.getNextOccurrence.mockReturnValue(new Date(Date.now() + 24 * 60 * 60 * 1000));
   165	  }, [mockAlarms, userTier]);
   166	
   167	  return (
   168	    <AnalyticsProvider>
   169	      <LanguageProvider>
   170	        <FeatureAccessProvider userId={userId}>
   171	          <StrugglingSamProvider userId={userId}>{children}</StrugglingSamProvider>
   172	        </FeatureAccessProvider>
   173	      </LanguageProvider>
   174	    </AnalyticsProvider>
   175	  );
   176	};
   177	
   178	  beforeEach(() => {           <-- ERROR: Missing test globals and describe block
   179	    jest.clearAllMocks();
   180	    localStorage.clear();
   181	    mockGeolocation.getCurrentPosition.mockClear();
   182	  });

Error type: Expression expected
Cause: 1) mockScheduler variable not defined, 2) Missing test globals shim and proper test structure
Fix: Add test globals shim, define mockScheduler variable, wrap beforeEach in describe block